-- Component scripts are expected to return a table with
-- the three functions described below. Naturally, local
-- state can be declared before the return statement

return{
    -- Called on component creation
    on_start=function ()
    end,

    -- Called each frame/tick
    on_update=function ()
    end,

    -- Called on component destruction
    on_delete=function ()
    end
}
